import "./Form.scss";

const Form = () => {
  return (
    <section className="contect-section">
      <section className="section-space text-center">Form Page</section>
    </section>
  );
};

export default Form;
